# Loading asdf from git install
if [ -d $HOME/.asdf ] ; then
  source $HOME/.asdf/asdf.sh
fi

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

source ~/.google-cloud
source ~/.python

if [ -d $HOME/.cargo/ ] ; then
  source $HOME/.cargo/env
fi
